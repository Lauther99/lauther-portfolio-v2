import React from 'react';
import '../assets/styles/contact.css'

const Contact = ({pageVisibility}) => {
    return (
        <div id='contact' className={`${pageVisibility}`}>
            spun un Contact
        </div>
    );
};

export default Contact;