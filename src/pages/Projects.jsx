import React from 'react';
import '../assets/styles/projects.css'

const Projects = ({pageVisibility}) => {
    return (
        <div id='projects' className={`${pageVisibility}`}>
            soy un project
        </div>
    );
};

export default Projects;