import React from 'react';
import '../assets/styles/skills.css'

const Skills = ({pageVisibility}) => {
    return (
        <div id='skills' className={`${pageVisibility}`}>
            soy un skills
        </div>
    );
};

export default Skills;